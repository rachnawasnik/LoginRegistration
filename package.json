
{
    "name": "LoginRegistration",
    "version": "0.1.0",
    "scripts": {
     // "postinstall": "npm install --prefix frontend && npm install --prefix expressapp"
        "postinstall": "npm install --prefix expressapp"
    }
  }
